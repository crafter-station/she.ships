import type { FilterSettings } from "./types";

export const DEFAULT_FILTER: FilterSettings = {
  bgBlur: 6,
  bgGrain: 0.14,
  faceGrain: 0.1,
  overlay: true,
  autoPosition: false,
  panX: 0,
  panY: 0,
  zoom: 1.0,
};
